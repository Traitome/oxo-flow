# Single-cell analysis with Seurat (gallery 09).
name: seurat
channels:
  - conda-forge
  - bioconda
dependencies:
  - r-base=4.3
  - r-seurat=5.1.0
  # Read10X_h5 (HDF5 feature-barcode matrices) needs hdf5r alongside Seurat.
  - r-hdf5r=1.3.12
  - r-tidyverse=2.0.0
