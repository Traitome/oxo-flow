{
  "id": "s04-silent-failure",
  "surface": "report",
  "run_expect_exit": 0,
  "expect_any": ["zcat", "can'?t stat", "no such file", "sample_r1\\.fastq"],
  "forbid": [],
  "setup": {},
  "ground_truth": "zcat fails (input absent) but the pipeline exit code comes from wc, so the rule reports success with a poisoned (empty) output; stderr_tail records the zcat error"
}
