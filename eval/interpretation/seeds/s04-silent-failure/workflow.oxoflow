[workflow]
name = "fastq-summary"
version = "0.1.0"
description = "Summarize read counts per sample"

[[rules]]
name = "summarize"
input = ["data/reads/sample_R1.fastq.gz"]
output = ["out/summary.txt"]
shell = "zcat {input[0]} | wc -l > {output[0]}"
